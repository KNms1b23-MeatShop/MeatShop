name: CI Code Check

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      
name: Checkout code
      uses: actions/checkout@v4

      
name: Simulate tests
      run: echo "Simulating tests... OK!"